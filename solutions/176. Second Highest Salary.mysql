# Write your MySQL query statement below
SELECT * FROM (
SELECT DISTINCT SALARY AS SecondHighestSalary  FROM (
SELECT
SALARY,
DENSE_RANK() OVER(ORDER BY SALARY DESC) AS RN
FROM EMPLOYEE) TAB
WHERE RN=2
UNION
SELECT NULL AS SecondHighestSalary ) FINAL
LIMIT 1
